{
  "name": "newton-cache",
  "version": "1.3.0",
  "description": "Lightweight cache library with pluggable adapters. Zero dependencies, TTL support, TypeScript-first.",
  "keywords": [
    "cache",
    "file-cache",
    "flat-file-cache",
    "memory-cache",
    "filesystem",
    "storage",
    "ttl",
    "key-value",
    "adapter",
    "file-based",
    "in-memory",
    "temporary",
    "zero-dependencies"
  ],
  "homepage": "https://github.com/skydiver/newton-cache#readme",
  "bugs": {
    "url": "https://github.com/skydiver/newton-cache/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skydiver/newton-cache.git"
  },
  "license": "MIT",
  "author": {
    "name": "Martín M.",
    "url": "https://github.com/skydiver/"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build && npm test",
    "test": "npm run build && node --test \"dist/__tests__/**/*.js\"",
    "test:coverage": "npm run build && c8 -r text -r lcov node --test \"dist/__tests__/**/*.js\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "@types/node": "^26.0.1",
    "c8": "^11.0.0",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
