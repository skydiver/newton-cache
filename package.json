{
  "name": "@skydiver/node-cache",
  "version": "0.1.0",
  "description": "Lightweight, dependency-free cache library with pluggable adapters. File-based storage with TTL support.",
  "keywords": [
    "cache",
    "file-cache",
    "filesystem",
    "storage",
    "ttl",
    "key-value",
    "adapter",
    "file-based",
    "temporary",
    "zero-dependencies"
  ],
  "homepage": "https://github.com/skydiver/node-cache#readme",
  "bugs": {
    "url": "https://github.com/skydiver/node-cache/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skydiver/node-cache.git"
  },
  "license": "MIT",
  "author": {
    "name": "Martín M.",
    "url": "https://github.com/skydiver/"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build && npm test",
    "test": "npm run build && node --test \"dist/__tests__/*.js\"",
    "test:coverage": "npm run build && c8 -r text -r lcov node --test \"dist/__tests__/*.js\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@types/node": "^24.0.0",
    "c8": "^9.1.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
