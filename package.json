{
  "name": "node-cache",
  "version": "0.1.0",
  "description": "Small, dependency-free file-based cache that stores plain text entries under a tmp directory.",
  "license": "MIT",
  "author": {
    "name": "Martín M.",
    "url": "https://github.com/skydiver/"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "test": "node --test dist",
    "test:coverage": "npm run build && c8 -r text -r lcov node --test dist"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@types/node": "^24.0.0",
    "c8": "^9.1.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
